language: python
python:
  - "3.3"

# Dependencies.
install:
  # Only for testing.
  - pip install -r requirements_testing.txt
# Run tests.
script:
  - nosetests
# Run coveralls.
after_success:
  - coveralls
