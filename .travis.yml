language: python
python:
  - "3.3"

# Dependencies.
install:
  # Only for testing.
  - pip install nose coverage coveralls
# Run tests.
script:
  - nosetests
# Run coveralls.
after_success:
  - coveralls
